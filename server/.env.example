PORT=4000
MONGO_URI=mongodb://localhost:27017/clearpath
JWT_SECRET=change-me-in-production

# Optional: extra frontend URLs (comma-separated, no trailing slashes).
# Localhost:5173 is always allowed. Add Vercel previews or custom domains, e.g.:
# CORS_ORIGINS=https://my-app.vercel.app,https://branch-name.vercel.app
