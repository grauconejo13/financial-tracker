# Backend origin (no /api suffix required — the app adds /api paths).
VITE_API_URL=http://localhost:4000

# Production (Vercel): set in Vercel env to your Render/hosted API, e.g.:
# VITE_API_URL=https://your-api.onrender.com
